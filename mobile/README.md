# 考匠移动端

这是考匠的 Expo / React Native 移动端。Android 当前版本为 v0.3.1；当前源码继续复用现有 Node 后端，通过移动端 Bearer 会话连接生产 API。

## 本地预览

```powershell
cd mobile
npm install
npm start
```

然后：

- Android 真机安装 Expo Go，扫描终端二维码；
- 已安装 Android Studio 时，可以按 `a` 启动 Android 模拟器；
- 需要连接真实后端时，复制 `.env.example` 为 `.env.local`，将 `EXPO_PUBLIC_API_URL` 改成电脑在局域网中的地址。

App 保留开发期“进入预览模式”入口，用于查看动画和页面结构；真实登录通过移动端 Bearer 会话接入，证书选择后会请求首页学习数据。生产构建对应 `kaojiang-v0.3.1.apk`。启动时会后台请求 `/api/mobile/version`，低于服务端最低版本的旧 App 会强制显示最新版下载页；旧客户端调用受保护的移动端 API 也会被服务端拒绝并返回下载地址。

版本过旧时，Android App 会优先提供应用内下载并调起系统安装器；系统仍会要求用户确认安装。页面同时保留浏览器下载入口，应用内下载失败或设备限制安装时可以使用浏览器方式。登录会话会保存在本机，正常关闭并重新打开 App 不需要重复登录；主动退出登录会清除本机会话。答题页采用紧凑布局，举报题目入口位于题干卡片右下角的三角感叹号图标。

## v0.3.1 更新

- 交互音效：轻柔、清脆、醇厚三种音色，可关闭、调整音量或试听，偏好保存在本机。
- 底部新增 VIP 套餐展示，社区和排行榜位于“我的 → 考匠社区”；支付及实时会员额度尚未接入。
- 已共享的 AI 题组会计入提交题目排行榜。
- Android versionCode 为 15，沿用现有安装包签名以支持覆盖升级。

## 当前页面

- 今日：学习概览、连续学习、今日任务；开始今日学习可选择知识范围，随机抽取 30 道题，不足时自动补题
- 我的：账号与证书信息、错题/收藏/已生成题目/学习进度快捷入口，以及分组后的账户和应用设置
- 练习：各级分类展示进度；支持顺序刷题、随机刷题和按具体知识点 AI 生成题目；图片题、简答题可直接作答
- AI 出题：每批生成 10 道，由服务端后台执行；离开页面或关闭 App 后任务仍继续，悬浮进度可在其他练习中查看。复核分批并行处理以缩短等待，每题仍须独立通过二次审核；未通过会继续补题直到凑足 10 道。生成的题组可上传或直接练习，答题前不展示答案
- AI 服务：AI 提示、错因解析、详细讲解和流式变式训练
- 题目反馈：举报答案/解析、题干/选项或重复题
- 错题：展示当前账号完整错题列表和知识点分布，可从具体条目直接开始复习
- 社区（我的 → 考匠社区）：公共大群支持文字、Emoji 和图片；可查看刷题量、正确率、连续天数、提交题目四个排行榜
- VIP：展示三档套餐和权益；支付及实时额度显示尚未开通
- 考试：考试模式入口占位
- 我的：账号、证书、社区昵称、动画、主题与音效设置、服务地址、赞助作者、版本信息和退出登录

社区图片使用 Expo 相册选择器上传，单张图片最大 6MB。服务端可通过 `COMMUNITY_STORAGE_LIMIT_BYTES` 调整总聊天存储上限，默认值为 2147483648（2GB）。

## 缓存与数据刷新

底部模块首次访问时创建，之后保留页面、滚动位置和当前作答。隐藏页面或 App 进入后台时停止社区轮询和循环动效；练习期间首页统计只标记过期，回到首页再更新。返回题库目录、显式开启另一组练习以及切换账号/证书时重置对应练习。内容立即可见，不再等待延迟淡入。

`src/api/resourceCache.ts` 统一管理请求合并、过期刷新、失效通知和存储队列；`cachePolicy.ts` 限定可落盘内容。缓存按 API 地址、账号 ID、证书 ID 隔离，切换作用域时释放旧内容；退出登录会清除学习缓存。保留最多 80 个内存请求结果，磁盘最多 24 项、约 1.5 MiB，并自动淘汰旧内容。

- 知识点目录、题量/进度摘要和访问过的普通顺序练习页可存入手机。60 秒内复用结果，之后先显示已有内容并后台刷新；超过 7 天丢弃。题目缓存按字段白名单保存，排除答案、解析和个人 AI 生成题。
- 首页统计、错题和收藏仅作短期内存缓存；聊天记录、AI 草稿、作答文本和 AI 配置不会写入学习缓存。登录会话仍使用独立的现有会话存储，不能把学习缓存当作凭据存储。
- 答题、收藏、移除错题、AI 出题提交成功后，使相关缓存失效。普通网络请求 30 秒超时；旧账号、旧证书和已失效请求的返回结果不能覆盖新状态。
- 目录、首页、错题支持下拉刷新；“我的 → 清理学习缓存”可手动清理。断网时可阅读已缓存资料，提交答案、收藏和 AI 等操作仍需联网；失败不会提示已经提交成功。

练习每批读取 40 题，剩余不超过 8 题时预加载下一批；随机分页使用同一个种子保持顺序稳定，避免重复抽取。后端 `/api/questions?page=1` 提供分页元数据，`/api/dashboard?summary=1` 返回轻量统计；未使用新参数的网页与旧客户端继续使用原返回格式。这次源码需要配套更新后端后再发布 App。

## 缓存回归验证

在项目根目录运行：

```powershell
node mobile/node_modules/typescript/bin/tsc --noEmit -p mobile
node --test test/mobile-cache.test.js test/mobile-client.test.js test/mobile-api.test.js
```

浏览器回归使用临时数据库和本地接口，不访问生产账户：

```powershell
cd mobile
$env:EXPO_PUBLIC_API_URL = 'http://127.0.0.1:5186/api'
node node_modules/expo/bin/cli export --platform web --output-dir ../test-output/mobile-web
cd ..
node test/mobile.e2e.js
```

浏览器回归验证模块返回不重复取题、作答保留、统计更新和断网重启读取缓存；它不能代替 Android/iOS 真机的帧率、后台恢复和安装验证。
